## 1. Shared Sorting Base

- [x] 1.1 Create a shared frontend sorting module for `date` / `name` sort mode types and comparator helpers.
- [x] 1.2 Update explorer sort control and section components to import and use the shared sort mode/helpers while preserving existing defaults.

## 2. Dashboard Sorting UI

- [x] 2.1 Add local date-default sort state and sort controls to dashboard Active Changes and Recent Activity section headers.
- [x] 2.2 Apply shared date/name sorting to dashboard Active Changes.
- [x] 2.3 Apply shared date/name sorting to dashboard Recent Activity, preserving the existing 12-item cap after date-based activity collection.

## 3. Validation

- [x] 3.1 Verify TypeScript/Svelte checks pass.
- [x] 3.2 Verify OpenSpec change status is apply-ready and requirements are covered.
